name: Expense Tracker

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Installing npm
        uses: actions/setup-node@v2-beta
        with:
          version: 12    

<<<<<<< HEAD
      - name: Installing yarn
=======
      - name: Installing yarn
>>>>>>> 54148668ae324b618efcb382a5619defb6209551
        uses: Borales/actions-yarn@v2.3.0
        with:
          cmd: install
          
      - name: Creating build
        run: yarn build
        
      - name: Installing surge
        run: npm install -g surge
      
      - name: Deploying on surge
        run: surge ./build myproject001.surge.sh --token ${{ secrets.MYPROJECT001 }}
    

